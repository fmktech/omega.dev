import type { RenderHtmlApp } from "../contracts/index.js";

const APP = String.raw`<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>omega.dev — local harness lab</title>
<style>
:root{color-scheme:dark;--bg:#090b0d;--panel:#111519;--raised:#181e23;--line:#263039;--text:#edf3f4;--muted:#8e9ba4;--lime:#b9f27c;--cyan:#7ce2f2;--red:#ff8b8b;--amber:#ffd083;--radius:14px;font:14px/1.45 Inter,ui-sans-serif,system-ui,sans-serif}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 10% -10%,#193323 0,transparent 32%),var(--bg);color:var(--text)}button,input,select,textarea{font:inherit}button{cursor:pointer}.unlock{min-height:100vh;display:grid;place-items:center;padding:24px}.unlock-card{width:min(440px,100%);padding:34px;border:1px solid var(--line);border-radius:22px;background:rgba(17,21,25,.94);box-shadow:0 26px 90px #0009}.mark{display:flex;align-items:center;gap:11px;font-weight:750;letter-spacing:-.03em;font-size:22px}.orb{width:27px;height:27px;border-radius:50%;background:conic-gradient(var(--lime),var(--cyan),#9d8cff,var(--lime));box-shadow:0 0 30px #b9f27c55}.unlock-card h1{font-size:38px;line-height:1.06;letter-spacing:-.05em;margin:42px 0 10px}.muted{color:var(--muted)}label{display:block;color:var(--muted);font-size:12px;margin:17px 0 7px}.field,select,textarea{width:100%;border:1px solid var(--line);border-radius:9px;background:#0c1013;color:var(--text);padding:10px 12px;outline:none}.field:focus,select:focus,textarea:focus{border-color:var(--lime)}.primary,.ghost,.danger{border-radius:9px;padding:9px 13px;border:1px solid transparent;font-weight:650}.primary{background:var(--lime);color:#13200c}.ghost{background:transparent;border-color:var(--line);color:var(--text)}.danger{background:#321b1d;border-color:#633337;color:#ffb3b3}.wide{width:100%;margin-top:16px}.shell{display:none;min-height:100vh;grid-template-columns:230px 1fr}.sidebar{position:sticky;top:0;height:100vh;border-right:1px solid var(--line);padding:22px 14px;background:#0c0f12}.sidebar .mark{padding:0 10px 24px}.nav{display:grid;gap:3px}.nav button{text-align:left;border:0;border-radius:9px;background:transparent;color:var(--muted);padding:9px 11px}.nav button:hover,.nav button.active{background:var(--raised);color:var(--text)}.sidebar-foot{position:absolute;bottom:18px;left:14px;right:14px}.status{display:flex;align-items:center;gap:8px;color:var(--muted);padding:9px}.dot{width:8px;height:8px;border-radius:50%;background:var(--amber)}.dot.ok{background:var(--lime)}.dot.bad{background:var(--red)}.content{min-width:0}.topbar{height:68px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);padding:0 28px;background:#090b0dcc;backdrop-filter:blur(12px);position:sticky;top:0;z-index:3}.crumb{font-weight:650}.project-picker{width:260px}.main{padding:28px;max-width:1320px;margin:auto}.view{display:none}.view.active{display:block}.heading{display:flex;justify-content:space-between;align-items:flex-start;gap:20px;margin-bottom:22px}.heading h1{margin:0;font-size:28px;letter-spacing:-.035em}.heading p{margin:5px 0 0;color:var(--muted)}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:14px}.card{border:1px solid var(--line);border-radius:var(--radius);background:var(--panel);padding:16px;min-width:0}.card h3{margin:0 0 5px;font-size:15px}.metric{font-size:29px;font-weight:720;letter-spacing:-.04em;margin-top:12px}.pill{display:inline-flex;border-radius:999px;padding:3px 8px;font-size:11px;background:#1d272d;color:var(--cyan)}.pill.good{color:var(--lime);background:#1d2b1d}.pill.warn{color:var(--amber);background:#302719}.toolbar{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:14px}.split{display:grid;grid-template-columns:minmax(280px,.75fr) minmax(360px,1.25fr);gap:14px}.stack{display:grid;gap:10px}.item{padding:12px;border:1px solid var(--line);border-radius:10px;background:#0e1215}.item:hover{border-color:#40505c}.item-title{display:flex;justify-content:space-between;gap:12px;font-weight:650}.mono{font:12px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere}.empty,.loading,.error{border:1px dashed var(--line);border-radius:var(--radius);padding:32px;text-align:center;color:var(--muted)}.error{border-color:#633337;color:#ffb3b3}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0 12px}.form-grid .full{grid-column:1/-1}.actions{display:flex;gap:7px;margin-top:11px}.terminal{height:360px;overflow:auto;background:#060809;border:1px solid var(--line);border-radius:12px;padding:13px;font:12px/1.55 ui-monospace,monospace;white-space:pre-wrap}.stream-state{display:flex;gap:8px;align-items:center;margin-bottom:9px;color:var(--muted)}.toast{position:fixed;right:20px;bottom:20px;max-width:430px;padding:12px 15px;border:1px solid var(--line);border-radius:11px;background:var(--raised);box-shadow:0 12px 50px #0009;display:none;z-index:9}.toast.error{display:block;padding:12px 15px;text-align:left}.modal{border:1px solid var(--line);border-radius:16px;background:var(--panel);color:var(--text);width:min(620px,calc(100% - 30px));padding:22px}.modal::backdrop{background:#000b}.raw{max-height:420px;overflow:auto;white-space:pre-wrap}.binary{padding:20px;border:1px dashed var(--line);border-radius:10px;color:var(--amber)}@media(max-width:850px){.shell{grid-template-columns:1fr}.sidebar{position:static;height:auto;border-right:0;border-bottom:1px solid var(--line)}.sidebar .mark{padding-bottom:12px}.nav{display:flex;overflow:auto}.nav button{white-space:nowrap}.sidebar-foot{position:static;margin-top:10px}.topbar{top:0}.split{grid-template-columns:1fr}.main{padding:18px}.form-grid{grid-template-columns:1fr}.form-grid .full{grid-column:auto}}
</style></head>
<body>
<section id="unlock" class="unlock"><form id="unlock-form" class="unlock-card"><div class="mark"><span class="orb"></span>omega.dev</div><h1>Your harness,<br>getting better.</h1><p class="muted">Connect to the local daemon. The token lives only in this page's memory and is forgotten on refresh.</p><label for="token">Daemon bearer token</label><input id="token" class="field mono" type="password" required autocomplete="off" spellcheck="false"><button class="primary wide">Unlock workspace</button><p id="unlock-error" class="muted" role="alert"></p></form></section>
<section id="shell" class="shell"><aside class="sidebar"><div class="mark"><span class="orb"></span>omega.dev</div><nav class="nav" id="nav"><button data-view="overview" class="active">Overview</button><button data-view="projects">Projects</button><button data-view="sessions">Sessions</button><button data-view="policy">Policy</button><button data-view="harnesses">Harnesses</button><button data-view="knowledge">Knowledge</button><button data-view="marketplace">Marketplace</button><button data-view="evolution">Evolution</button><button data-view="scorecards">Scorecards</button></nav><div class="sidebar-foot"><div class="status"><span id="health-dot" class="dot"></span><span id="health-text">Checking daemon…</span></div><button id="lock" class="ghost wide">Lock</button></div></aside><div class="content"><header class="topbar"><div id="crumb" class="crumb">Overview</div><select id="project-picker" class="project-picker"><option value="">Choose a project</option></select></header><main class="main">
<section id="view-overview" class="view active"><div class="heading"><div><h1>Harness control room</h1><p>Local sessions, evidence, and evolution at a glance.</p></div><button class="primary" data-go="sessions">Start task</button></div><div id="overview-grid" class="grid"><div class="loading">Loading your workspace…</div></div><div class="heading" style="margin-top:28px"><div><h1>Live activity</h1><p>Persisted events and ephemeral deltas.</p></div></div><div class="stream-state"><span id="stream-dot" class="dot"></span><span id="stream-label">Select a session to connect</span></div><div id="artifact-actions" class="toolbar"></div><div id="terminal" class="terminal">No session selected.</div></section>
<section id="view-projects" class="view"><div class="heading"><div><h1>Projects</h1><p>Each repository owns its active harness lineage.</p></div><button class="ghost" data-refresh="projects">Refresh</button></div><div class="split"><div><div id="projects-list" class="stack"><div class="loading">Loading projects…</div></div></div><form id="register-form" class="card"><h3>Register workspace</h3><p class="muted">The daemon derives repository identity from the path.</p><label>Absolute path</label><input name="path" class="field mono" placeholder="/home/me/project" required><button class="primary wide">Register</button><div id="registered-workspace" class="muted mono" style="margin-top:10px"></div></form></div></section>
<section id="view-sessions" class="view"><div class="heading"><div><h1>Sessions</h1><p>Start fresh work or continue from a handoff.</p></div><button class="ghost" data-refresh="sessions">Refresh</button></div><div class="split"><div id="sessions-list" class="stack"><div class="empty">Choose a project.</div></div><div class="stack"><form id="start-form" class="card"><h3>Start a task</h3><label>Workspace ID</label><input name="workspace" class="field mono" required><label>Objective</label><textarea name="objective" rows="4" required></textarea><button class="primary wide">Start and stream</button></form><form id="resume-form" class="card"><h3>Resume from handoff</h3><div class="form-grid"><div><label>Source session</label><input name="source" class="field mono" required></div><div><label>Workspace ID</label><input name="workspace" class="field mono" required></div><div><label>Handoff artifact</label><input name="handoff" class="field mono" required></div><div><label>Context artifact IDs</label><input name="contexts" class="field mono" placeholder="comma,separated"></div></div><button class="ghost wide">Resume and stream</button></form></div></div></section>
<section id="view-policy" class="view"><div class="heading"><div><h1>Execution policy</h1><p>Resolve side effects that require operator judgment.</p></div><div class="toolbar"><select id="policy-state"><option>pending</option><option>resolved</option></select><button class="ghost" data-refresh="policy">Refresh</button></div></div><label>Session ID</label><input id="policy-session" class="field mono" style="max-width:520px;margin-bottom:14px"><div id="policy-list" class="stack"><div class="empty">Enter a session ID.</div></div></section>
<section id="view-harnesses" class="view"><div class="heading"><div><h1>Harness lineage</h1><p>Inspect, pin, and roll back project-scoped versions.</p></div><button class="ghost" data-refresh="harnesses">Refresh</button></div><div id="harness-list" class="grid"><div class="empty">Choose a project.</div></div></section>
<section id="view-knowledge" class="view"><div class="heading"><div><h1>Project knowledge</h1><p>Short summaries first; open a document when you need depth.</p></div></div><form id="knowledge-form" class="toolbar"><input name="text" class="field" style="max-width:420px" placeholder="Search decisions, paths, techniques"><input name="tags" class="field" style="max-width:220px" placeholder="tags,comma,separated"><button class="primary">Search</button></form><div id="knowledge-list" class="grid"><div class="empty">Choose a project and search.</div></div></section>
<section id="view-marketplace" class="view"><div class="heading"><div><h1>Local marketplace</h1><p>Harness parts created and vetted by your own projects.</p></div></div><form id="market-form" class="toolbar"><input name="text" class="field" style="max-width:390px" placeholder="Search tools, connectors, skills"><select name="state"><option value="">All trusted states</option><option>experimental</option><option>proven</option><option>deprecated</option></select><button class="primary">Search</button></form><div id="market-list" class="grid"><div class="empty">Search the local catalog.</div></div></section>
<section id="view-evolution" class="view"><div class="heading"><div><h1>Evolution jobs</h1><p>Diagnose hard-won solutions, mutate candidates, evaluate evidence.</p></div><button class="ghost" data-refresh="evolution">Refresh</button></div><div class="split"><div id="evolution-list" class="stack"><div class="empty">Choose a project.</div></div><form id="evolution-form" class="card"><h3>Start evolution</h3><label>Source session ID</label><input name="source" class="field mono" required><label>Goal</label><textarea name="goal" rows="3" required></textarea><label>Evidence artifact IDs</label><input name="evidence" class="field mono" placeholder="comma,separated"><label>Allowed component kinds</label><input name="kinds" class="field" value="runner,tool,skill,workflow"><button class="primary wide">Create candidate</button></form></div></section>
<section id="view-scorecards" class="view"><div class="heading"><div><h1>Promotion scorecards</h1><p>Paired evidence stays model-marked and comparable.</p></div><button class="ghost" data-refresh="scorecards">Refresh</button></div><form id="paired-form" class="toolbar"><input name="suite" class="field mono" style="max-width:210px" placeholder="Suite ID" required><input name="incumbent" class="field mono" style="max-width:230px" placeholder="Incumbent harness" required><input name="candidate" class="field mono" style="max-width:230px" placeholder="Candidate harness" required><button class="primary">Run paired eval</button></form><div id="scorecard-list" class="stack"><div class="empty">Choose a project.</div></div></section>
</main></div></section><div id="toast" class="toast" role="status"></div><dialog id="detail" class="modal"><div class="heading"><div><h1 id="detail-title">Details</h1></div><button id="detail-close" class="ghost">Close</button></div><div id="detail-body" class="raw mono"></div></dialog>
<script>
"use strict";
const CONFIG=__OMEGA_CONFIG__;
let token="",projectId="",projects=[],sessions=[],streamController=null,lastSequence=0;
const $=(q)=>document.querySelector(q), esc=(v)=>String(v).replace(/[&<>"']/g,(c)=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const csv=(v)=>String(v||"").split(",").map(x=>x.trim()).filter(Boolean);
function notify(message,error=false){const node=$("#toast");node.textContent=message;node.className="toast"+(error?" error":"");node.style.display="block";setTimeout(()=>node.style.display="none",3600)}
function rid(){return crypto.randomUUID()}
async function call(kind,fields={}){const response=await fetch(CONFIG.requestPath,{method:"POST",headers:{authorization:"Bearer "+token,"content-type":"application/json",accept:"application/json"},body:JSON.stringify({kind,requestId:rid(),...fields})});if(!response.ok){let message="Daemon returned HTTP "+response.status;try{const body=await response.json();message=body.error?.message||message}catch{}throw new Error(message)}const body=await response.json();if(!body.result?.ok)throw new Error(body.result?.error?.message||body.result?.error?.reason||body.result?.error?.kind||"Request failed");return body.result.value}
async function paged(kind,fields){const items=[];let cursor=null;do{const value=await call(kind,{...fields,page:{cursor,limit:100}});items.push(...value.page.items);cursor=value.page.nextCursor}while(cursor!==null);return items}
function showDetail(title,value){$("#detail-title").textContent=title;$("#detail-body").textContent=JSON.stringify(value,null,2);$("#detail").showModal()}
function setView(name){document.querySelectorAll(".view").forEach(n=>n.classList.toggle("active",n.id==="view-"+name));document.querySelectorAll("#nav button").forEach(n=>n.classList.toggle("active",n.dataset.view===name));$("#crumb").textContent=name[0].toUpperCase()+name.slice(1);if(["sessions","harnesses","evolution","scorecards"].includes(name))refresh(name)}
function item(title,subtitle,badge,actions=""){return '<div class="item"><div class="item-title"><span>'+esc(title)+'</span><span class="pill">'+esc(badge)+'</span></div><div class="muted mono">'+esc(subtitle)+'</div>'+actions+'</div>'}
function state(target,value){$(target).innerHTML=value}
async function loadProjects(){state("#projects-list",'<div class="loading">Loading projects…</div>');const all=[];let cursor=null;do{const value=await call("project.list",{page:{cursor,limit:50}});all.push(...value.page.items);cursor=value.page.nextCursor}while(cursor!==null);projects=all;const picker=$("#project-picker");picker.innerHTML='<option value="">Choose a project</option>'+all.map(p=>'<option value="'+esc(p.id)+'">'+esc(p.displayName)+'</option>').join("");picker.value=projectId;if(!all.length)state("#projects-list",'<div class="empty">No projects yet. Register a workspace to begin.</div>');else state("#projects-list",all.map(p=>item(p.displayName,p.id,p.activeHarnessId?"ready":"initializing",'<div class="actions"><button class="ghost" data-select-project="'+esc(p.id)+'">Select</button></div>')).join(""));return all}
async function loadSessions(){if(!projectId)return state("#sessions-list",'<div class="empty">Choose a project.</div>');state("#sessions-list",'<div class="loading">Loading sessions…</div>');const all=[];let cursor=null;do{const value=await call("session.list",{projectId,page:{cursor,limit:50}});all.push(...value.page.items);cursor=value.page.nextCursor}while(cursor!==null);sessions=all;if(!all.length)return state("#sessions-list",'<div class="empty">No sessions for this project.</div>');state("#sessions-list",all.map(s=>{const version=s.header.eventSchemaVersion;const badge=version===1?s.state:"schema v"+version;return item(s.header.objective,s.header.id,badge,'<div class="actions"><button class="ghost" data-stream="'+esc(s.header.id)+'" data-sequence="'+esc(s.lastSequence)+'">Stream</button><button class="ghost" data-detail-session="'+esc(s.header.id)+'">Inspect</button>'+(s.state==="running"||s.state==="waiting"?'<button class="danger" data-cancel-session="'+esc(s.header.id)+'">Cancel</button>':"")+'</div>')}).join(""))}
async function loadPolicy(){const sid=$("#policy-session").value.trim();if(!sid)return state("#policy-list",'<div class="empty">Enter a session ID.</div>');state("#policy-list",'<div class="loading">Loading decisions…</div>');const records=await paged("policy.list",{sessionId:sid,state:$("#policy-state").value});if(!records.length)return state("#policy-list",'<div class="empty">No '+esc($("#policy-state").value)+' escalations.</div>');state("#policy-list",records.map(e=>item(e.reason,e.id,e.state,e.state==="pending"?'<div class="actions"><button class="primary" data-policy="'+esc(e.id)+'" data-resolution="allow">Allow</button><button class="danger" data-policy="'+esc(e.id)+'" data-resolution="deny">Deny</button></div>':"")).join(""))}
async function loadHarnesses(){if(!projectId)return state("#harness-list",'<div class="empty">Choose a project.</div>');state("#harness-list",'<div class="loading">Loading harnesses…</div>');const records=await paged("harness.list",{projectId});if(!records.length)return state("#harness-list",'<div class="empty">No harness versions found.</div>');state("#harness-list",records.map(h=>'<div class="card"><span class="pill">'+esc(h.alias)+'</span><h3 class="mono">'+esc(h.id)+'</h3><p class="muted">'+h.components.length+' components · '+esc(h.createdAt)+'</p><div class="actions"><button class="ghost" data-detail-harness="'+esc(h.id)+'">Inspect</button><button class="primary" data-harness-action="pin" data-harness="'+esc(h.id)+'">Pin</button><button class="danger" data-harness-action="rollback" data-harness="'+esc(h.id)+'">Rollback</button></div></div>').join(""))}
async function loadEvolution(){if(!projectId)return state("#evolution-list",'<div class="empty">Choose a project.</div>');state("#evolution-list",'<div class="loading">Loading evolution jobs…</div>');const records=await paged("evolution.list",{projectId});if(!records.length)return state("#evolution-list",'<div class="empty">No evolution jobs yet.</div>');state("#evolution-list",records.map(j=>item(j.request.goal,j.id,j.state,'<div class="actions"><button class="ghost" data-detail-evolution="'+esc(j.id)+'">Inspect</button>'+(!["promoted","rejected","cancelled","failed"].includes(j.state)?'<button class="danger" data-cancel-evolution="'+esc(j.id)+'">Cancel</button>':"")+'</div>')).join(""))}
async function loadScorecards(){if(!projectId)return state("#scorecard-list",'<div class="empty">Choose a project.</div>');state("#scorecard-list",'<div class="loading">Loading scorecards…</div>');const records=await paged("scorecard.list",{projectId});if(!records.length)return state("#scorecard-list",'<div class="empty">No promotion evidence yet.</div>');state("#scorecard-list",records.map(s=>item(s.decision.reason,s.id,s.decision.outcome,'<div class="actions"><button class="ghost" data-detail-scorecard="'+esc(s.id)+'">Evidence</button></div>')).join(""))}
const loadTargets={projects:"#projects-list",sessions:"#sessions-list",policy:"#policy-list",harnesses:"#harness-list",evolution:"#evolution-list",scorecards:"#scorecard-list"};
async function refresh(name){try{if(name==="projects")await loadProjects();else if(name==="sessions")await loadSessions();else if(name==="policy")await loadPolicy();else if(name==="harnesses")await loadHarnesses();else if(name==="evolution")await loadEvolution();else if(name==="scorecards")await loadScorecards();renderOverview()}catch(e){const target=loadTargets[name];if(target)state(target,'<div class="error">'+esc(e.message||"Could not load")+'<div class="actions" style="justify-content:center"><button class="ghost" data-refresh="'+esc(name)+'">Retry</button></div></div>');notify(e.message||"Could not load",true)}}
function renderOverview(){const running=sessions.filter(s=>s.state==="running"||s.state==="waiting").length;const active=projects.find(p=>p.id===projectId);state("#overview-grid",'<div class="card"><span class="muted">Projects</span><div class="metric">'+projects.length+'</div></div><div class="card"><span class="muted">Sessions loaded</span><div class="metric">'+sessions.length+'</div></div><div class="card"><span class="muted">Running now</span><div class="metric">'+running+'</div></div><div class="card"><span class="muted">Active harness</span><div class="metric mono" style="font-size:14px">'+esc(active?.activeHarnessId||"Select a project")+'</div></div>')}
function appendEvent(event){const terminal=$("#terminal");if(terminal.textContent==="No session selected.")terminal.textContent="";if(event.kind==="session-event"&&event.event.payload.kind==="artifact.recorded"){const artifact=event.event.payload.artifact;$("#artifact-actions").insertAdjacentHTML("beforeend",'<button class="ghost" data-artifact="'+esc(artifact.id)+'">Open '+esc(artifact.kind)+'</button>')}const line=event.kind==="model-delta"&&(event.event.kind==="text-delta"||event.event.kind==="reasoning-delta")?event.event.delta:JSON.stringify(event);terminal.textContent+=line+(event.kind==="model-delta"?"":"\n");terminal.scrollTop=terminal.scrollHeight}
function terminalStreamError(message){const error=new Error(message);error.terminal=true;return error}
function parseSseBlock(block,sessionId){let id=null,event=null,data=[];for(const line of block.split(/\r?\n/)){if(!line||line[0]===":")continue;const at=line.indexOf(":"),field=at<0?line:line.slice(0,at),value=at<0?"":line.slice(at+1).replace(/^ /,"");if(field==="id")id=value;else if(field==="event")event=value;else if(field==="data")data.push(value)}if(event!=="omega.live"||id===null||!data.length)throw terminalStreamError("Unsupported stream frame");let envelope;try{envelope=JSON.parse(data.join("\n"))}catch{throw terminalStreamError("Malformed stream payload")}if(envelope.sessionId!==sessionId)throw terminalStreamError("Stream session mismatch");return {id,envelope}}
function lockWorkspace(message="Workspace locked"){if(streamController)streamController.abort();token="";projectId="";$("#token").value="";$("#shell").style.display="none";$("#unlock").style.display="grid";$("#unlock-error").textContent=message}
async function stream(sessionId,after=0){if(streamController)streamController.abort();streamController=new AbortController();lastSequence=after;$("#terminal").textContent="";$("#artifact-actions").textContent="";let attempt=0;while(!streamController.signal.aborted){$("#stream-label").textContent=attempt?"Reconnecting…":"Live · "+sessionId;$("#stream-dot").className="dot "+(attempt?"":"ok");try{const url=CONFIG.eventsPath.replace(":sessionId",encodeURIComponent(sessionId))+"?afterSequence="+lastSequence;const response=await fetch(url,{headers:{authorization:"Bearer "+token,accept:"text/event-stream"},signal:streamController.signal});if(response.status===401){lockWorkspace("Daemon token expired. Unlock again.");return}if(!response.ok){if(response.status>=400&&response.status<500)throw terminalStreamError("Stream returned HTTP "+response.status);throw new Error("Stream returned HTTP "+response.status)}const reader=response.body.getReader(),decoder=new TextDecoder();let buffer="";while(true){const part=await reader.read();if(part.done)break;buffer+=decoder.decode(part.value,{stream:true});let match;while((match=/\r?\n\r?\n/.exec(buffer))!==null){const block=buffer.slice(0,match.index);buffer=buffer.slice(match.index+match[0].length);if(!block.trim()||block.trim()[0]===":")continue;const frame=parseSseBlock(block,sessionId);if(frame.envelope.kind==="session-event"){const seq=frame.envelope.event.sequence;if(seq<=lastSequence)continue;lastSequence=seq}appendEvent(frame.envelope)}}attempt=0}catch(e){if(streamController.signal.aborted)break;if(e.terminal){$("#stream-label").textContent=e.message;$("#stream-dot").className="dot bad";notify(e.message,true);return}attempt++;$("#stream-label").textContent="Reconnecting after interruption…";$("#stream-dot").className="dot";await new Promise(r=>setTimeout(r,Math.min(250*2**attempt,4000)))}}}
$("#unlock-form").addEventListener("submit",async e=>{e.preventDefault();token=$("#token").value;try{const response=await fetch(CONFIG.healthPath);const health=await response.json();if(!response.ok||health.protocolVersion!==1)throw new Error("Incompatible daemon protocol");await loadProjects();$("#unlock").style.display="none";$("#shell").style.display="grid";$("#health-dot").className="dot ok";$("#health-text").textContent="Local daemon online";renderOverview()}catch(err){token="";$("#token").value="";$("#unlock-error").textContent=err.message||"Could not connect"}});
$("#lock").addEventListener("click",()=>lockWorkspace());$("#detail-close").addEventListener("click",()=>$("#detail").close());
$("#nav").addEventListener("click",e=>{const view=e.target.dataset.view;if(view)setView(view)});document.addEventListener("click",async e=>{const t=e.target;try{if(t.dataset.go)setView(t.dataset.go);if(t.dataset.refresh)await refresh(t.dataset.refresh);if(t.dataset.selectProject){projectId=t.dataset.selectProject;$("#project-picker").value=projectId;await loadSessions();renderOverview()}if(t.dataset.stream)void stream(t.dataset.stream,Number(t.dataset.sequence||0));if(t.dataset.cancelSession){await call("session.cancel",{sessionId:t.dataset.cancelSession,reason:"Cancelled from local dashboard"});notify("Session cancelled");await loadSessions()}if(t.dataset.policy){await call("policy.resolve",{request:{escalationId:t.dataset.policy,resolution:t.dataset.resolution,reason:"Resolved from local dashboard"}});notify("Policy decision recorded");await loadPolicy()}if(t.dataset.harnessAction){await call("harness."+t.dataset.harnessAction,{projectId,targetHarnessId:t.dataset.harness,reason:t.dataset.harnessAction+" from local dashboard"});notify("Harness updated");await loadProjects();await loadHarnesses()}if(t.dataset.cancelEvolution){await call("evolution.cancel",{jobId:t.dataset.cancelEvolution,reason:"Cancelled from local dashboard"});await loadEvolution()}if(t.dataset.detailSession){showDetail("Session",(await call("session.get",{sessionId:t.dataset.detailSession})).session)}if(t.dataset.detailHarness){showDetail("Harness",(await call("harness.get",{harnessId:t.dataset.detailHarness})).harness)}if(t.dataset.detailEvolution){showDetail("Evolution",(await call("evolution.get",{jobId:t.dataset.detailEvolution})).job)}if(t.dataset.detailScorecard){showDetail("Scorecard",(await call("scorecard.get",{scorecardId:t.dataset.detailScorecard})).scorecard)}if(t.dataset.knowledge){showDetail("Knowledge document",(await call("knowledge.read",{projectId,documentId:t.dataset.knowledge})).document)}if(t.dataset.artifact){const value=await call("artifact.read",{artifactId:t.dataset.artifact,offset:0,limit:65536});const slice=value.slice;$("#detail-title").textContent="Artifact";$("#detail-body").innerHTML=slice.encoding==="base64"?'<div class="binary">Binary artifact · base64 · '+esc(slice.range.endExclusive-slice.range.startInclusive)+' bytes</div>':esc(slice.data);$("#detail").showModal()}}catch(err){notify(err.message||"Action failed",true)}});
$("#project-picker").addEventListener("change",async e=>{projectId=e.target.value;await refresh("sessions")});
$("#register-form").addEventListener("submit",async e=>{e.preventDefault();try{const value=await call("project.register-workspace",{path:new FormData(e.target).get("path")});$("#registered-workspace").textContent="Workspace ID: "+value.workspace.id;projectId=value.project.id;await loadProjects();$("#project-picker").value=projectId;notify("Workspace registered")}catch(err){notify(err.message,true)}});
$("#start-form").addEventListener("submit",async e=>{e.preventDefault();if(!projectId)return notify("Choose a project first",true);try{const f=new FormData(e.target),value=await call("task.start",{request:{projectId,workspaceId:f.get("workspace"),objective:f.get("objective"),modelRole:"main-coder"}});await loadSessions();setView("overview");void stream(value.session.header.id,0)}catch(err){notify(err.message,true)}});
$("#resume-form").addEventListener("submit",async e=>{e.preventDefault();try{const f=new FormData(e.target),value=await call("thread.resume",{request:{sourceSessionId:f.get("source"),workspaceId:f.get("workspace"),handoffArtifactId:f.get("handoff"),contextArtifactIds:csv(f.get("contexts"))}});await loadSessions();setView("overview");void stream(value.session.header.id,0)}catch(err){notify(err.message,true)}});
$("#knowledge-form").addEventListener("submit",async e=>{e.preventDefault();if(!projectId)return notify("Choose a project first",true);try{const f=new FormData(e.target),value=await call("knowledge.catalog",{query:{projectId,text:f.get("text"),tags:csv(f.get("tags")),relevantPaths:[],limit:50}});state("#knowledge-list",value.entries.length?value.entries.map(d=>'<div class="card"><span class="pill good">'+Math.round(d.confidence*100)+'% confidence</span><h3>'+esc(d.title)+'</h3><p class="muted">'+esc(d.summary)+'</p><button class="ghost" data-knowledge="'+esc(d.id)+'">Open document</button></div>').join(""):'<div class="empty">No matching knowledge.</div>')}catch(err){notify(err.message,true)}});
$("#market-form").addEventListener("submit",async e=>{e.preventDefault();try{const f=new FormData(e.target),s=f.get("state"),value=await call("marketplace.search",{query:{text:f.get("text"),kinds:[],states:s?[s]:["experimental","proven","deprecated"],limit:100}});state("#market-list",value.artifacts.length?value.artifacts.map(a=>'<div class="card"><span class="pill">'+esc(a.kind)+' · '+esc(a.state)+'</span><h3>'+esc(a.title)+'</h3><p class="muted">'+esc(a.summary)+'</p><button class="ghost" onclick=\'document.querySelector("#detail-body").textContent='+JSON.stringify(JSON.stringify(a,null,2))+';document.querySelector("#detail").showModal()\'>Inspect</button></div>').join(""):'<div class="empty">Nothing matched.</div>')}catch(err){notify(err.message,true)}});
$("#evolution-form").addEventListener("submit",async e=>{e.preventDefault();if(!projectId)return notify("Choose a project first",true);try{const f=new FormData(e.target),budget={wallTimeMs:3600000,maxModelCalls:128,maxInputTokens:1000000,maxOutputTokens:256000,maxCostUsdMicros:0,maxProcessStarts:64};await call("evolution.start",{request:{projectId,sourceSessionId:f.get("source"),goal:f.get("goal"),evidenceArtifactIds:csv(f.get("evidence")),allowedComponentKinds:csv(f.get("kinds")),budget}});notify("Evolution job started");await loadEvolution()}catch(err){notify(err.message,true)}});
$("#paired-form").addEventListener("submit",async e=>{e.preventDefault();try{const f=new FormData(e.target),value=await call("benchmark.run-paired",{suiteId:f.get("suite"),incumbentId:f.get("incumbent"),candidateId:f.get("candidate")});showDetail("Promotion scorecard",value.scorecard);await loadScorecards()}catch(err){notify(err.message,true)}});
$("#policy-state").addEventListener("change",()=>void loadPolicy());$("#policy-session").addEventListener("change",()=>void loadPolicy());
</script></body></html>`;

export const renderHtmlApp: RenderHtmlApp = (config) => {
  const publicConfig = {
    requestPath: config.requestPath,
    healthPath: config.healthPath,
    eventsPath: config.sessionEventsPathTemplate,
    protocolVersion: config.protocolVersion,
  };
  return APP.replace("__OMEGA_CONFIG__", JSON.stringify(publicConfig).replaceAll("<", "\\u003c"));
};
